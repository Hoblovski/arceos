被测的 API

```
libax::env::{
	current_dir(), set_current_dir()
}
fs::{
	create_dir(), 
	remove_dir(), 
	remove_file(), 
	metadata(),
}
metadata.{
	is_dir(),
	len(),
	file_type(),
	permissions(),
}
File::{
	open(),
	create(),
}
file.{
	read(), write(),
}
```
